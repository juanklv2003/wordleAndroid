<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Color de fondo semitransparente (Hex #AA es la opacidad) -->
    <solid android:color="#AA333333" />

    <!-- Bordes redondeados -->
    <corners android:radius="16dp" />

    <!-- Relleno interno (opcional) -->
    <padding
        android:left="10dp"
        android:top="5dp"
        android:right="10dp"
        android:bottom="5dp" />
</shape>